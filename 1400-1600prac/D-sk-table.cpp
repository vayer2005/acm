#include <bits/stdc++.h>
#define mp make_pair

using namespace std;
typedef long long ll;


void solve() {
    int n; int q;
    cin >> n >> q;

    while (q--) {
        string v;
        cin >> v;
        if (v == "->") {
            ll x; ll y;
            cin >> x >> y;

            
        } else {

        }
    }

}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while (t--) {
        solve();
    }

}