#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

void solve() {
    ll n; ll m;
    cin >> n >> m;

    ll res = max(n,m) + 1;
    cout << res << endl;
    

}

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);
    int t;
    cin >> t;
    while (t--) {
        solve();
    }

}